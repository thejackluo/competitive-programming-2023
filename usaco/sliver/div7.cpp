#include <cstdio>
#include <bits/stdc++.h>
using namespace std;

int main() {
    // fast input output
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // files
    freopen("div7.in", "r", stdin);
	freopen("div7.out", "w", stdout);
    
    // input
    int n;
    cin >> n;

    // input n lines of numbers that could be 0 to 1,000,000
    int a[n];
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }


    // output
    return 0;
}
